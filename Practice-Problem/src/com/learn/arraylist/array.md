Methods in ArrayList => add, addAll, remove, removeAll, clear, contains, size, get, set, indexOf, iterator

- 🔥 MOST USED IN REAL PROJECTS
- add 
- addAll 
- get 
- set(index, element)
- size 
- contains
- remove(object) ...(more common than remove(index))
- clear
- iterator() 

❌ occasionally(situation based) used 
- add(index, e) 
- indexOf (big lists me avoid)

✅ Checkpoint (interview-ready)
- contains, indexOf → equals()
- remove(index) vs remove(object)
- clear() capacity reduce nahi karta